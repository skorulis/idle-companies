//  Created by Alexander Skorulis on 2/8/2022.

import Foundation
/*
enum AlloyActivity: String, POperation, Identifiable, CaseIterable {
    
    case bronze
    
    var skill: Skill { .metallurgy }
    var id: String { rawValue }
}

// MARK: - Activity defintion

extension AlloyActivity {
    
    var name: String { output.name }
    var icon: ThemeIcon { output.icon }
    
    var output: ItemType {
        switch self {
        case .brick: return .brick
        case .concrete: return .concrete
        }
    }
}
*/
